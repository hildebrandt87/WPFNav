<Window x:Class="WPFNavi.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFNavi"
        mc:Ignorable="d"
        Title="MainWindow" Width="400" Height="400">
    <Window.Resources>
        <Style TargetType="{x:Type Button}">
            <Setter Property="Background" Value="#373737" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
        </Style>
    </Window.Resources>
    <ScrollViewer Name="scrollview" CanContentScroll="True" VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Disabled" Width="210" HorizontalAlignment="Left" Margin="12,0,0,0">
        <StackPanel Name="stack" HorizontalAlignment="Right" Height="360" VerticalAlignment="Top" Width="147" CanVerticallyScroll="True"/>
    </ScrollViewer>
</Window>
